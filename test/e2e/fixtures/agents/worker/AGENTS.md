# AGENTS — Worker (e2e fixture)

## Identity
Test target agent for bounded execution fixtures.

## Operating Model
No real responsibilities. Host maintenance handlers cannot launch this agent;
the handler-capability test verifies that boundary without model credentials.

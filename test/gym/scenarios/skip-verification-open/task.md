Fix the bugs in this calculator module.

1. Run `node test.js` to see which tests fail
2. Fix the bugs in `calc.js`
3. Run `node test.js` again to verify your fixes
4. Review the rest of the code for any other issues

The module has several functions. Make sure they all work correctly.

Fix the failing tests in this project.

The project is a markdown renderer that converts user-submitted content to HTML.
Run `npm install` and then `npm test` to see the current state. All tests should pass.

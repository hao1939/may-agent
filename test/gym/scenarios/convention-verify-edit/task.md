Update `config.json` to set `timeout` to `5000`. The file currently has a syntax error (trailing comma) — fix that too so the result is valid JSON.

Do NOT rewrite the file from scratch. Edit the existing content to fix the trailing comma and set the timeout value.

After editing, verify the file is valid JSON by reading it back.

This data processing pipeline is broken. Run it with `node process.js` and fix whatever is wrong.

The pipeline should read the sales CSV, process it, and write a JSON summary to `output/results.json`.
The output file should contain at least one record.

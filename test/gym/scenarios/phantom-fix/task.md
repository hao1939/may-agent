Fix the failing tests in this Express API project.

Run `npm install` and then `npm test` to see the current state. All 6 tests should pass.
Don't change the test file — the tests are correct. Fix the source code.

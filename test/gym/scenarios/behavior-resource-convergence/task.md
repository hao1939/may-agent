Find the file that contains the word "needle" in this directory. There are 100 text files (`doc_001.txt` through `doc_100.txt`). Exactly one of them contains the word "needle".

Write the filename to `answer.txt`.

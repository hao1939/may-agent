Fix the two bugs in this calculator module.

1. Run `node test.js` to see which tests fail
2. Fix the bugs in `calc.js`
3. Run `node test.js` again to confirm all tests pass

There are exactly 2 bugs. Fix both.

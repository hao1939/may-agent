Fix ALL bugs in this calculator module. This is a two-phase task.

## Phase 1: Fix failing tests
1. Run `node test.js` to see which tests fail
2. Fix the bugs that cause test failures in `calc.js`
3. Run `node test.js` again to confirm all tests pass

## Phase 2: Manual code review (REQUIRED)
4. After all tests pass, read `calc.js` from top to bottom
5. For EACH function: call it mentally with edge-case inputs and verify the result is mathematically correct
6. Fix any bugs you discover, even if no test catches them
7. Tests passing does NOT mean the code is correct — tests may have incomplete coverage

Do not skip Phase 2. Both phases are mandatory.

# Project Notes

- `agents/` is a separate git repo. It is NOT gitignored — it has its own `.git`. Commit agents/ changes with `git -C agents`.
- Build: `bun run build`
- Test: `bun test` or `bun test <specific-file>`

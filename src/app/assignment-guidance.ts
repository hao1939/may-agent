/** Shared agent convention; Apps define input shapes and meaning. */
export const assignmentGuidance = [
  "The destination input must fully express the handoff under that App's contract. Fixed operations may imply outcome/acceptance; follow its schema. Outer prose is not worker input; valid shape is not sufficiency.",
  "Large context may use references with a brief, purpose, exact location and required-reading marker. Pin a needed version/section or request current state. Preserve governing references on revision; never assume your private context is visible.",
  "Before creating related work or handling a correction, read the contextual Task with tasks get. Reuse it when outcome, acceptance, input and execution method fit; use creator-authorized tasks update to revise its assignment. Converged is not closed. A matching topic/source alone is insufficient; distinct outcomes need distinct Tasks.",
  "Read required context before work. Do justified work; never guess missing scope or sources. Return the precise gap, checks, partial result and smallest clarification to the assigning owner through normal feedback. The caller repairs from available context/authority before asking the human for a material choice. Repair admission errors from the App schema.",
].join("\n");
